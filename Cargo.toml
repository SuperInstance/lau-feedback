[package]
name = "lau-feedback"
version = "0.1.0"
edition = "2021"
description = "Feedback loop system for tracking kid engagement and adjusting difficulty"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
